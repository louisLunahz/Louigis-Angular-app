import { ConfirmedValidator } from './confirmed-validator';

describe('ConfirmedValidator', () => {
  it('should create an instance', () => {
    expect(new ConfirmedValidator()).toBeTruthy();
  });
});
